{
  "name": "pollex-extension",
  "version": "1.8.1",
  "private": true,
  "description": "Pollex Chrome extension — test tooling (Vitest unit + Playwright e2e)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "vitest": "^3.0.5"
  }
}
